.reaction-button-container.reaction-label
	| reactions
.reaction-button-container.reaction-button.more-reactions(data-value="thumbsup")
	.em.em-thumbsup
	if counts && counts['thumbsup']
		.badge= counts['thumbsup']
.reaction-button-container.reaction-button.more-reactions(data-value="thumbsdown")
	.em.em-thumbsdown
	if counts && counts['thumbsdown']
		.badge= counts['thumbsdown']
.reaction-button-container.reaction-button.more-reactions(data-value="smiley")
	.em.em-smiley
	if counts && counts['smiley']
		.badge= counts['smiley']
.reaction-button-container.reaction-button.more-reactions(data-value="smirk")
	.em.em-smirk
	if counts && counts['smirk']
		.badge= counts['smirk']
.reaction-button-container.reaction-button.more-reactions(data-value="yum")
	.em.em-yum
	if counts && counts['yum']
		.badge= counts['yum']
.reaction-button-container.reaction-button.more-reactions(data-value="heart_eyes")
	.em.em-heart_eyes
	if counts && counts['heart_eyes']
		.badge= counts['heart_eyes']
.reaction-button-container.reaction-button.more-reactions(data-value="worried")
	.em.em-worried
		if counts && counts['worried']
			.badge= counts['worried']
.reaction-button-container.reaction-button.more-reactions(data-value="cry")
	.em.em-cry
		if counts && counts['cry']
			.badge= counts['cry']
.reaction-button-container.reaction-button.more-reactions(data-value="angry")
	.em.em-angry
		if counts && counts['angry']
			.badge= counts['angry']
.reaction-button-container.reaction-button.more-reactions(data-value="rage")
	.em.em-rage
		if counts && counts['rage']
			.badge= counts['rage']
.pull-right
	if reactions
		for reaction in reactions
			.reaction
				- var thumb = reaction.resolvedProfiles[reaction.source].profile.photo.url
				img.profile-thumb(src= thumb)
